node_modules/
dist/

# Generated JSDoc site — jsdoc.json points its destination at ./docs/js, and the
# tree also carries vendored third-party libraries (fuse, hljs, popper, tippy,
# tocbot). Reformatting either loses on the next `pnpm run docs:js`.
docs/js/

# Build output owned by webpack; regenerated by `pnpm run build`.
assets/
build/
build_test/
dest_test/

# Composer packages.
vendor/

# Written by tooling, not by hand — formatting these only fights the generator.
pnpm-lock.yaml
CHANGELOG.md
.release-please-manifest.json
